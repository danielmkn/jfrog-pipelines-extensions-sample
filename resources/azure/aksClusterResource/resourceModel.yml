configuration:
  aksResourceGroup:
    type: String
  aksClusterName:
    type: String
  location:
    type: String
  vmSize:
    type: String
  numNodes:
    type: Int
  minCount:
    type: Int
  maxCount:
    type: Int
  # Cluster options is a set of parameters for 'az aks create', described in the Azure documentation:
  # https://docs.microsoft.com/en-us/cli/azure/aks?view=azure-cli-latest#az_aks_create
  clusterOptions:
    type: String